import styled from 'styled-components';

const StyledProposalSearch = styled.div`
  margin: 15px 0;

  div {
    display: flex;
    flex-direction: column;
  }

  div label {
    margin-top: 0;
  }
`;

export default StyledProposalSearch;
