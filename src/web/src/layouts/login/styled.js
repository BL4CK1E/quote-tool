import styled from 'styled-components';

const StyledAuthorization = styled.div`
    height:100vh;
    display: -ms-flexbox;
    display: flex;
    -ms-flex-direction: column;
    flex-direction: column;
    justify-content:center;
    align-items:center;
`;

export default StyledAuthorization;
